#include <stdio.h>
#include <stdlib.h>

int main()
{
    int v, e;
    printf("Enter the number of vertex:\t");
    scanf("%d", &v);

    printf("Enter the number of edges:\t");
    scanf("%d", &e);

    int edges[v][v];
    int weight[v][v];

    printf("Enter the details of the given graph:\t");
    scanf
    for(int i = 0; i < e; i++)
    {
        int src, dest, weight;
    }
}
